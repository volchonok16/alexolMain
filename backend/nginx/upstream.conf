# Общий upstream для api.alexol.io и be.alexol.io
# На сервере: положить в /etc/nginx/conf.d/ или sites-enabled/ (должен грузиться до server-блоков)

upstream alexol_backend {
        server 127.0.0.1:8547;
        keepalive 32;
}
